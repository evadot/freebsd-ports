--- meson.build.orig	2023-11-20 10:10:12 UTC
+++ meson.build
@@ -1787,7 +1787,10 @@ endif
   dep_version = cpp.find_library('version')
 endif
 
-dep_elf = dependency('libelf', required : false)
+# Do not depend on libelf from devel/elfutils
+# We will fallback on our libelf
+#dep_elf = dependency('libelf', required : false)
+dep_elf = null_dep
 if not with_platform_windows and not dep_elf.found()
   dep_elf = cc.find_library('elf', required : false)
 endif
